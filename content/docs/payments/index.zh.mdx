---
title: 支付系统概览
description: 使用 Creem 的订阅计费。
---

## 概述

SceneSelf 使用 [Creem](https://creem.io) 进行支付处理,提供三档周期性订阅。

## 订阅计划

在 `constants/billing.ts` 中配置:

| 计划 | 价格 | 积分 | 周期 |
|------|------|------|------|
| Weekly | $9.90 | 1,500 | 每周 |
| Monthly *(推荐)* | $29 | 8,000 | 每月 |
| Yearly | $299 | 100,000 | 每年 |

`RECOMMENDED_PLAN`(默认 `"monthly"`)控制定价页"最受欢迎"高亮。

## 支付流程

1. **用户点击购买** → 前端调用 `/api/payments/creem/checkout`
2. **创建 Checkout Session** → Creem API 返回支付链接
3. **用户完成支付** → Creem 发送 webhook 到 `/api/payments/creem/webhook`
4. **Webhook 处理**:
   - 签名验证(HMAC-SHA256)
   - 幂等性检查(防止重复处理)
   - 插入支付记录
   - 创建/更新订阅记录
   - 发放积分(瞬间到账)
   - 写 `subscription_credit_schedule`(供 cron 续期发放)
   - 发送确认邮件

## 周期续费

三档全部 `per_cycle` 一次性发放,**不分期**:
- Weekly: Creem 每周自动续费 → webhook → +1,500 积分
- Monthly: Creem 每月自动续费 → webhook → +8,000 积分
- Yearly: Creem 每年自动续费 → webhook → +100,000 积分

`subscription-grants` cron 是防御性兜底(目前 3 档不依赖,但建议跑以防 webhook 失败时补偿)。

## 测试

在 `.env` 中设置 `CREEM_SIMULATE="true"` 可跳过真实支付,方便开发测试。
