import { SceneAuthLayout } from "@/layouts/scene-auth-layout";

export default function AuthXLayout({ children }: { children: React.ReactNode }) {
  return <SceneAuthLayout>{children}</SceneAuthLayout>;
}
